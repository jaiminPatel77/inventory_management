import { AllocationGroupPipe } from './allocation-group.pipe';

describe('AllocationGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new AllocationGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
